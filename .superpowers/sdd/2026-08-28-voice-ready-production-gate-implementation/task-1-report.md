Status: complete (fix round 2)

Commit: recorded with this report (`feat: add recoverable voice-ready phase`)

Implementation: inserted `voice_ready` into the persisted phase order and both phase schemas. New projects now durably initialize at schema version 2, so their logs are distinguishable from the immutable version-one pre-voice history. New `append_event` calls continue to enforce only the revised order. Replay recognizes the single historical version-one transition difference (`direction_ready` to `storyboard_ready`) while retaining every event-contract, locking, fsync, and symlink safeguard; a version-two persisted direct transition is rejected. A legacy version-one project at `direction_ready` that advances to `voice_ready` first appends the durable `project.schema_upgraded` v2 event, then the phase event. Replay reconstructs the schema-valid v2 snapshot without rewriting any old JSONL line. The event contract is checked before this append so a malformed voice request cannot leave a partial upgrade. `project_recovery_view` is read-only: post-direction snapshots without a supplied valid voice-lineage predicate normalize to `direction_ready` with `voice-artifacts-required`, leaving JSONL bytes untouched. Its optional predicate boundary deliberately prevents `project_state` from importing the forthcoming `voice` module.

TDD evidence: added the new phase-order and immutable legacy-recovery tests, then observed RED from the missing `project_recovery_view` import. Added a tampered legacy-event regression, observed RED because the compatibility path accepted an extra event field, then constrained that path to the exact event contract. In fix round 1, the version-two initialization and persisted-tamper regressions were RED against the version-one-only implementation, then passed after the distinguishable version boundary was added. In fix round 2, the legacy-upgrade, event-schema, and project-template tests were RED before the durable upgrade event and v2 template existed; the malformed-request regression was RED when it exposed a partial-upgrade write, then passed after contract validation moved before the upgrade append.

Verification: `PYTHONPYCACHEPREFIX=/tmp/voice-ready-task1-green-contract python3 -m unittest tests.test_project_state -v` — 16 passed. `PYTHONPYCACHEPREFIX=/tmp/voice-ready-task1-fix1-green python3 -m unittest tests.test_project_state tests.test_end_to_end -v` — 53 passed. `PYTHONPYCACHEPREFIX=/tmp/voice-ready-task1-fix1-full python3 -m unittest discover -s tests -v` — 222 passed. `PYTHONPYCACHEPREFIX=/tmp/voice-ready-task1-fix2-green-contract python3 -m unittest tests.test_project_state tests.test_validation tests.test_package tests.test_end_to_end -v` — 95 passed. `PYTHONPYCACHEPREFIX=/tmp/voice-ready-task1-fix2-full python3 -m unittest discover -s tests -v` — 225 passed. `PYTHONPYCACHEPREFIX=/tmp/voice-ready-task1-fix2-compile python3 -m py_compile scripts/toolkit/*.py scripts/*.py tests/*.py` — passed. `python3 scripts/validate_package.py` — `package valid`. `git diff --check` — passed.

Fix attribution: the earlier full-suite smoke failure was caused by Task 1's phase-order change, not a downstream defect. This round updates the existing metadata-only resume smoke to append `voice_ready` before `storyboard_ready`; it intentionally does not create synthetic voice artifacts, which remains later voice-smoke work.

Concerns: Task 5 must call `project_recovery_view` with Task 2's authoritative lineage predicate when resuming projects; the default is intentionally conservative.
